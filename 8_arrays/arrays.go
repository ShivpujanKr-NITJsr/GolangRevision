package main

// import "fmt"

func main() {
	// var nums [4]int // declare an array of integers with size 4

	// nums[0] = 10 // assign values to the array

	// println(nums[0]) // print the first element of the array
	// println(nums)
	// array length
	// println(len(nums))
	//for int it is 0 by default
	// var vals [4]bool
	// vals[0] = true // assign values to the array
	// fmt.Println(vals) // prints [false false false false]
	// by default empty string=""
	// var name [3]string // declare an array of strings with size 3
	// name[0] = "John"   // assign values to the array
	// fmt.Println(name)  // prints [John  ]

	// nums := [3]int{1, 2, 3} // declare and initialize an array of integers
	// fmt.Println(nums)              // prints [1 2 3 4 5]

	// 2d arrays
	// arr := [2][3]int{{}}  // or int {{3,4,5}, {6,7,8}}
		
	// fmt.Println(arr) // prints [[0 0 0] [0 0 0]]


	//  -fixed size -predictable
	// memory optimization
	// contant time access
}
